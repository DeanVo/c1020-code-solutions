/* eslint-disable no-unused-vars */

function equal(first, second) {
  first = first.join(' ');
  second = second.join(' ');

  if (first === second) {
    return true;
  } else {
    return false;
  }
}
